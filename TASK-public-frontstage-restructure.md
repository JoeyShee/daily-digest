# CC Task: Daily Digest 公开前台减法重构

## 角色与目标

你是执行 CC。主 Codex 负责产品和项目管理；你只负责按本任务实现，不重新定义产品方向。

将 Daily Digest 从七个同级系统入口收紧为一个低噪音公开判断产品。第一阶段只上线已经有成熟公开内容的两个入口：

1. `今日判断`：沿用现有 `perception/` Repricing 内容。
2. `10x研究`：沿用现有 `dual-innovation/` 成熟投资交付。

`生意案例`和`Build下注`是下一阶段目标；在没有通过证据门的首批内容前，不创建空栏目，也不把旧“经典案例”或 Zero2Idea 候选直接公开。

## 必须实现

- 将所有当前公开页面的频道导航收紧为两个入口：`今日判断`、`10x研究`。
- `商业感知`继续作为产品总品牌；默认页面/频道标题改为`今日判断`。
- 将公开显示的`双创研究`统一改为`10x研究`，但保持现有 `/dual-innovation/` URL，避免破坏旧链接。
- 停止日常渲染器继续生成或更新这些前台内容：`熵减计划`、`想法墓地`、`Zero2Idea`、旧`10x投机`、`信号库`。
- 旧目录和历史 HTML 必须保留，不删除，确保直接旧链接仍可访问；本轮只停止导航曝光和后续更新。
- 首页没有 Repricing 报告时，不得回退展示想法墓地或其他后台内容；应显示安静、诚实的“暂无值得占用注意力的新判断”状态。
- 更新 README 和相关脚本注释，使公开边界与实际行为一致。
- 如果存在独立维护的 10x 研究首页或其他生成入口，确保其导航与名称也一致。

## 明确禁止

- 不删除历史目录、HTML、原始数据或私有来源。
- 不把健康提醒、个人复盘、课程卡、工具榜单候选、Zero2Idea 候选、缺来源经典案例重新包装为公开内容。
- 不创建空的“生意案例”或“Build下注”页面。
- 不提交 Git、不 push、不触发 Cloudflare 或任何公网发布。
- 不修改 `/Users/joeyshee/Documents/10x投机`、`~/.hermes` 数据源或个人站部署仓库，除非只是读取以确认边界。

## 验收要求

- `python3 -m py_compile render.py` 通过。
- 本地渲染成功；生成的首页、`perception/index.html`、`dual-innovation/index.html` 导航只包含两个新标签。
- 上述公开页面不再包含导航标签：`熵减计划`、`想法墓地`、`Zero2Idea`、`双创研究`、`10x投机`、`信号库`。
- 运行今日渲染时，不再新增或更新上述五类后台/旧频道页面。
- 原有历史目录仍存在。
- 最终报告列出修改文件、验证命令和任何仍未解决的问题。

## 工作方式

- 先检查当前工作树和生成链，再做最小、可回退的修改。
- 保留用户已有改动，不覆盖无关内容。
- 若发现产品范围外的重大阻塞，停止并报告；不要自行扩张范围。
